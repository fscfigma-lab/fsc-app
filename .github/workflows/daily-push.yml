name: 🔔 Ежедневные уведомления (резерв)

on:
  workflow_dispatch:

# Этот файл оставлен для ручного запуска при необходимости.
# Основная логика уведомлений перенесена в web-push.yml —
# он запускается каждые 30 минут и сам проверяет время (09:00 и 18:00 МСК).

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Запустить web-push вручную
        run: echo "Для ручного запуска уведомлений используй workflow_dispatch на web-push.yml"
